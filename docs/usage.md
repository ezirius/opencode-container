# OpenCode container usage

## Workflow

1. Build the shared image:
   `./scripts/opencode-build`
2. Start a workspace container:
   `./scripts/opencode-start general`
3. Open OpenCode interactively:
   `./scripts/opencode-open general`
